# Flight_calculator
Used GitHub Copilot Chat (/tests) to generate the initial unit
test skeletons for calculate_flight_time(). Rewrote 2 of the
5 tests -- the originals didn't check the negative-weight
error case. Verified all tests pass with pytest.
